import { StyleSheet, Text, View } from "react-native";

const document = () => {
  return (
    <View>
      <Text>document</Text>
    </View>
  );
};

export default document;

const styles = StyleSheet.create({});
